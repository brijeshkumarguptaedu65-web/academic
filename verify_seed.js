const mongoose = require('mongoose'); const Chapter = require('./models/Chapter'); const Topic = require('./models/Metadata').Topic; require('dotenv').config(); mongoose.connect(process.env.MONGO_URI).then(async () => { const count = await Chapter.countDocuments(); console.log('Total Chapters:', count); const topics = await Topic.find(); console.log('Topics:', topics.map(t => t.name).join(', ')); process.exit(); });
